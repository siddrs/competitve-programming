#include <bits/stdc++.h>
using namespace std;

void solve(){

    int k, l, m, n, d, dmg;
    cin >> k;
    cin >> l;
    cin >> m;
    cin >> n;
    cin >> d;

    if (k == 1) cout << d;
    else {

    }

    

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    solve();

    return 0;

}